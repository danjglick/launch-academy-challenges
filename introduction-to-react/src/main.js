import './main.scss'
import React from 'react';
import ReactDOM from 'react-dom';
import Header from './components/Header'

ReactDOM.render(
  <Header />,
  document.getElementById('app')
);


/*
use react.js to set up jsx html-like elements
in the react.js file, use ReactDOM.render(jsx, html)
	to replace the html element with the jsx element
*/	